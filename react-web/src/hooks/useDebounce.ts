
function useDebounce(
  fn: (...param: any) => any,
  ms: number = 100,
  immediate: boolean = false) { 
  let counter = 0;
  setTimeout(() => { 
    
  })
}
export default useDebounce
function foo(data1:number,data2:string) { 
  return new Date();
}
useDebounce(foo)